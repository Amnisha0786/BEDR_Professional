export enum MESSAGE_CONTENT_TYPE {
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
  FILE = 'file',
}

export enum USER_ACTIVITY {
  ONLINE = 'Online',
  OFFLINE = 'Offline',
}

export enum MESSAGE_STATUS {
  READ = 'read',
  DELIVERED = 'delivered',
  SENT = 'sent',
}
